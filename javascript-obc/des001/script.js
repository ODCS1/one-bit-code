// Calcular a Hipotenusa

var a = 3;
var b = 4;

// window.prompt('Digite o valor cateto:')
// window.prompt('Digite o outro cateto:')

var c = Math.sqrt(Math.pow(a, 2) + Math.pow(b, 2))
console.log(c)
